# Introduction

Modern applications require robust access management to protect sensitive resources and user data.

Two key components enable secure access:

- **Authentication**
- **Authorization**

Authentication verifies the identity of a user attempting to access a system.

Authorization determines what resources that authenticated user is permitted to access.

Together, they create a layered security model used in modern applications, APIs, and cloud environments.